version: 1.0.{build}
image:
- Visual Studio 2015
- Visual Studio 2017
- Visual Studio 2019
environment:
  matrix:
    - platform: x86
      configuration: Release
    - platform: x64
      configuration: Release
matrix:
  fast_finish: false
build:
  verbosity: Minimal
  parallel: true
  project: sorttest.sln
