version: 1.0.{build}
image:
- Visual Studio 2015
- Visual Studio 2017
environment:
  matrix:
    - platform: x86
      solution_name: sorttest\sorttest.sln
      configuration: Release
      verbosity: Minimal
    - platform: x64
      solution_name: sorttest\sorttest.sln
      configuration: Release
      verbosity: Minimal
matrix:
  fast_finish: false
build_script:
  - msbuild %solution_name%